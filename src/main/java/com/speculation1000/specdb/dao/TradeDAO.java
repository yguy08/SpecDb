package com.speculation1000.specdb.dao;

import com.speculation1000.specdb.db.DbConnectionEnum;

public class TradeDAO {
	
	public TradeDAO(DbConnectionEnum dbce) {
		//sync with entries. 3 times a day, solidify (trades). 
	}

}
